/**
 * Who produced a turn. There is no SYSTEM member: the system prompt is supplied
 * fresh on every call rather than stored, so changing it takes effect at once.
 */
package com.shopassist.enums.chat;
