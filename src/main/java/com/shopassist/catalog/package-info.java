/**
 * Products and catalog search.
 *
 * <p>Public: browsing works without an account, as a real storefront does.
 *
 * <p>Availability is published as a band rather than a count. Stock levels are
 * commercially sensitive and no shopper needs the figure, so the catalog cannot
 * be scraped to reconstruct inventory or infer sales rates.
 */
package com.shopassist.catalog;
