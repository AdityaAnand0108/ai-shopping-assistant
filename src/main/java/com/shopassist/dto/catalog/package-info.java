/**
 * Catalog request and response types.
 *
 * <p>{@code ProductSearchCriteria} normalises and bounds every search
 * parameter in its constructor. The assistant's search tool builds the same
 * record, so model-proposed arguments face exactly the limits a hand-written
 * HTTP request does.
 */
package com.shopassist.dto.catalog;
