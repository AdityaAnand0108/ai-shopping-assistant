/**
 * Order response types.
 *
 * <p>{@code OrderDetailResponse} computes whether an order may still be
 * cancelled server-side, rather than leaving a client — or a model — to infer
 * it from the status.
 */
package com.shopassist.dto.order;
