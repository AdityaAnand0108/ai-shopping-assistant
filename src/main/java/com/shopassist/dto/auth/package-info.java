/**
 * Sign-up and sign-in payloads.
 *
 * <p>{@code SignupRequest} carries the Jakarta constraints that define a valid
 * account; {@code LoginRequest} deliberately does not repeat them, since telling
 * an attacker which guesses were malformed narrows the search for them.
 */
package com.shopassist.dto.auth;
