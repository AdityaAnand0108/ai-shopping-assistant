/**
 * JPA entities: the persistent model.
 *
 * <p>Surrogate keys never leave the server. Anything exposed over HTTP is
 * addressed by a random public reference, a SKU, or an order number.
 */
package com.shopassist.entity;
