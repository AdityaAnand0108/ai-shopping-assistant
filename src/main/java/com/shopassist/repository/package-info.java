/**
 * Spring Data repositories.
 *
 * <p>Owner-scoped repositories take the owner in the method signature rather
 * than leaving it to the caller. Ownership checks that live in a signature
 * cannot be forgotten at a call site — including by a tool the model invokes.
 */
package com.shopassist.repository;
