/**
 * Catalog queries: a keyword path that filters and pages in SQL, and a path
 * that applies the same hard filters to SKUs semantic retrieval proposed.
 */
package com.shopassist.repository.catalog;
