/**
 * HTTP entry points.
 *
 * <p>Controllers stay thin: they bind and validate a request, delegate, and
 * return a DTO. Every rule worth testing lives in {@code services} instead.
 */
package com.shopassist.controllers;
