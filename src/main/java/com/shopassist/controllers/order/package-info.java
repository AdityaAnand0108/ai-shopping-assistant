/**
 * Order history for the signed-in shopper.
 *
 * <p>No path carries a user identifier. Whose orders these are comes from the
 * token, so there is no URL to edit to reach somebody else's.
 */
package com.shopassist.controllers.order;
