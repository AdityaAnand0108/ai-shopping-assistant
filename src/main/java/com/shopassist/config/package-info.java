/**
 * Spring configuration and externalised settings.
 *
 * <p>Holds {@code @Configuration} classes and the {@code @ConfigurationProperties}
 * records they read, split by the area each configures.
 */
package com.shopassist.config;
