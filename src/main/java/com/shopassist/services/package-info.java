/**
 * Business logic. Every rule worth testing lives here rather than in a
 * controller, which is why the assistant's tools call these classes directly
 * and can therefore see no more than an HTTP client can.
 */
package com.shopassist.services;
